import { EpicSearchPipe } from './epic-search.pipe';

describe('EpicSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EpicSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
